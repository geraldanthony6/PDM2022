let eyeBallGroup = [];
let startTime;
let wave;
let numEyes = 30;
let gameState = 'startScreen';
let score = 0;
let timeLeft;
let initDirection = [-1, 1];

function preload() {
    eyes = loadImage('AngryEyeball-Sheet-Sheet.png');
}

function setup() {
    createCanvas(window.innerWidth, window.innerHeight);
    imageMode(CENTER);

    //for(int i = 0; i < numEyes; i++){
      //  eyeBallGroup[i] = 
    //}
}